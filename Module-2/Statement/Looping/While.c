// WAP to print 0 to 9 using while loop
#include <stdio.h>
int main()
{
    /*char i;
    i = 'a'; // initialize the variable with a character value of ‘A’
    while (i <= 'z')
    {
        printf("%c\n", i);
        ++i;// incrementing by one each time
        }
    */
//    ------------------------------------------------
    /*
     */
    int i;
    i = 10; // initialize the variable with 0
    while (i < 10)
    {
        printf("%d \n", i);
        ++i;// incrementing by one each time through the loop
    }
    return 0;
}