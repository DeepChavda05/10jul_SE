// WAP to addition of the two variable
#include<stdio.h>
int main()
{
    int a, b;
    int ans;
    printf("Enter the value of a and b: ");
    scanf("%d%d", &a, &b);
    // ans = a+b;
    printf("Addition of the a and b is: %d", a+b);
    return 0;
}