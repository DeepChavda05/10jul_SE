#include<stdio.h>
//Global variable(define outside the function)
int a = 100; //define variable
int main()
{
    //Local variable(define inside the function)
    int no = 46; //define variable
    printf("\nValue of no is: %d", no); //print the value
    return 0;
}
/*
    Static value = Compile time
    Dynamic Value = Run Time
*/