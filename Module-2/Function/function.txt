Function
-> Function is block of statement.

Types: 
//1) void function - return type is not specified, so it's a procedure (void).

1 built in:- 
->printf(), scanf()
------------------------------
2 UDF(user define function)
->which was define by user{float/*return type*/ main(int no)}

a) without return without parameter
b) without return with parameter
c) with return without parameter
d) with return with parameter

e.recursion(function call it self)
--------------------------------
return_type UDF_name (parameter)
int data ()
{
	return 5;
}